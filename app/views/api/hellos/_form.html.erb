<%= form_with(model: api_hello, local: true) do |form| %>
  <% if api_hello.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_hello.errors.count, "error") %> prohibited this api_hello from being saved:</h2>

      <ul>
      <% api_hello.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
